v37 is Python 2 - use setup.py to wrap as executable


v38+ is Python 3 - use setup3.py to wrap as executable
- Edit setup3.py to reflect new script name
- Delete the src/build directory
- Run setup thus:  >python3.6 setup3.py build
- Rename exe.win32-3.6/ to TMine99/ then zip
- send to user

## Install instructions for user (in README.txt):
dummy='''
-	Download file
-	Unzip somewhere, it should create a folder called TMine??
-	Copy your kill list and save list to the TMine??/utils/ directory.
-	Copy the executable TMiner??.exe that is in the directory
-	Go to the directory you want the shortcut (desktop perhaps?)
-	Right click and paste 'shortcut'
-	Right-click the short-cut it to get 'properties'
-	Make sure that the 'Start In' directory is TMine?? (or whatever you named the unzipped directory)
    '''
